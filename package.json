{
  "devDependencies": {
    "@commitlint/cli": "^20.4.2",
    "@commitlint/config-conventional": "^20.4.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "scripts": {
    "prepare": "husky",
    "dev": "docker network create traefik 2>/dev/null || true && docker compose -f infra/traefik/docker-compose.yml up -d && docker compose -f infra/preview/docker-compose.preview.yml --env-file .env --env-file infra/preview/.env up -d"
  }
}
